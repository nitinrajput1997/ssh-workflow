name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: test
        run: echo Hello, Nitin Rajput!

      - name: ssh
        run: |
          pwd
          cd /home/runner/.ssh
          mkdir authorized_keys
          cp {{ secrets.AUTH_KEY }} /home/runner/.ssh/authorized_keys
          cd
          ls
